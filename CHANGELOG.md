# 更新日志

## [1.0.0] — 2026-09-08

首个统一双平台的正式版本。桌面端（Windows）与 Android 移动端基于同一套源码构建。

### 新增：Android 移动端
- Android 版 MarkFlow（SAF 文件系统）：通过系统文件选择器打开 / 新建 / 另存为 Markdown 文件
- 编辑器的编辑 / 预览切换、自动草稿保存与崩溃恢复、未保存退出确认、系统返回键处理
- 基于 Tauri 2 移动端 + Kotlin FileAccessPlugin 的 SAF 桥接

### 新增（桌面端）
- 现代化界面：更紧凑的顶栏、标签栏与工具栏在 WYSIWYG 模式合并为一行、主页直启（启动不再残留上次的文件标签）
- 侧边栏新增关闭按钮

### 修复（桌面端）
- 修复命令注册被二次 `invoke_handler` 覆盖，导致文件打不开、窗口无法关闭的问题
- 修复图片粘贴被 fs 权限 scope 拒绝而写入失败的问题；图片现通过 Rust 命令保存到文档同级 `assets/`，并在预览 / 编辑器中经 asset protocol 正确显示
- 修复 WYSIWYG 打开文件即被自动改写格式的问题（仅真实用户输入后才回写内容）
- 修复跨标签撤销会把上一个文件的内容写回当前文件的问题
- 修复启动时主页"继续当前工作"失效、会话文件指针被清除的问题
- 修复外部文件变更时未保存标签被静默跳过、随后自动保存覆盖外部改动的问题
- 修复文件树重命名 / 删除后打开的标签不同步的问题
- 修复路径分隔符不一致导致同一文件被打开两次的问题
- 修复窗口关闭 / 会话保存 / 未命名草稿恢复的一系列边界问题

### 修复（Android）
- 修复保存失败后保存按钮永久禁用的状态卡死问题
- 修复草稿单槽互相覆盖、非原子写入损坏后无法恢复的问题（按文档 URI 分槽 + 原子写）
- 修复系统返回键绕过未保存确认直接退出应用的问题

### 工程
- 单一 `main` 分支维护桌面 + Android，one codebase、条件编译双平台
- 优化 `.gitignore`，排除所有构建产物，仓库内容可完整复现
- 新增 `write_binary_file`、`save_android_draft` 系列等后端命令

---

## 历史版本（Windows 桌面端）

### [1.0.3] — 桌面端稳定版（并入 1.0.0）

- 修复 Windows 会话文件无法可靠覆盖及损坏备份恢复问题
- 修复应用关闭流程重复拦截，导致窗口无法退出的问题
- 修复系统文件关联启动时，目标文件可能被会话恢复覆盖的问题
- 修复已有实例最小化后，二次启动无法唤醒窗口的问题
- 修复设置弹窗在较矮窗口中顶部和底部被裁切的问题
- 设置弹窗支持内部滚动，启动选项和快捷键使用响应式双列布局

### [1.0.2]

- 支持未命名标签自动落盘与异常退出恢复、外部文件变更检测、窗口位置记忆、文件关联启动、单实例文件转发
- 修复 session.json 原子写入、关闭最后一个标签回主页、打开文件占位标签等一系列问题
- 工程：统一 markdown 测试配置，新增 38 项 Node.js 逻辑测试

### [1.0.0] — 桌面端首发（2026-07-06）

- 所见即所得编辑（Milkdown / ProseMirror）、源码编辑（CodeMirror 6）、分栏 / 预览视图
- 多标签、文件树、目录大纲、AI 写作助手、会话持久化、导出 HTML/纯文本/PDF、搜索替换、明暗主题
- CommonMark 0.31.2 兼容性 647/651（99.4%）